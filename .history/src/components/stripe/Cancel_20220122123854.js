const Cancel = () => {
    const cancelStyle = {
        color: 'red',
      };

    return (
      <div>
        <h1 style={cancelStyle}>Cancel</h1>
        <h2>Your payment was canceled.</h2>
      </div>
    );
  };
  
  export default Cancel;
  